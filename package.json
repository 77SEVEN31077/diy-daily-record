{
  "name": "diy-daily-record",
  "version": "1.0.0",
  "description": "Privacy-first daily record PWA with local-first records and optional monthly leaderboard.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:firebase": "echo \"Firebase Hosting is not used for this project. Use Vercel only.\" && exit 1",
    "deploy:vercel": "npm run build && vercel --prod"
  },
  "keywords": [
    "daily-record",
    "pwa"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {}
}
